name: First Workflow

on:
  push:

jobs:

  first-job:

    runs-on: ubuntu-latest

    steps:

      - name: Welcome Message
        run: echo "Hello GitHub Actions"

      - name: Show Date
        run: date

      - name: List Files
        run: ls -la